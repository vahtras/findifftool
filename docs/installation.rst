.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install findifftool

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv findifftool
    $ pip install findifftool
